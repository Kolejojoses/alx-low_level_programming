#include "dog.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * new_dog - dog to be copied
 * @name: name of dog
 * @age: age of dog
 * @owner: owner of the dog
 * Return: new_dog
 */

dog_t *new_dog(char *name, float age, char *owner)
{
	if (new_dog = NULL)
		return (NULL);
	return (new_dog);
}
